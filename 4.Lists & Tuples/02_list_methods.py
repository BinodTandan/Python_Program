l1 = [1, 3, 2, 5, 4, 7, 6]
print(l1)
l1.sort() # sorting of the list
# l1.reverse() # Reversing the elements of the list
# l1.append(8) # Add element at the end of the array
# l1.insert(0, 0) # Insert the value in specefic index 
# l1.pop(5) # Remove element of specefic index
# l1.remove(6) # Remove elements that mentioned 
# l1.clear() # Remove all elements from the list
# l1.extend([10, 11, 12, 13]) # Add another list in one list
# l1.copy()
print(l1)
